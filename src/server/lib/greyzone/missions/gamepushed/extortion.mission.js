/*
  This mission is triggered when player owes money one of his agents might be taken
  and returned only after money is returned (agent effectively blocks one spot)
  Extortion is way to execute this, though events for this has to be handled separately
*/

const imgsrc = 'Extortion.jpg';
const sound = 'Extortion.ogg';
const tag = 'extortion';
const title = 'Extortion';

const Extortion = [{
  
}];

export default Extortion;
