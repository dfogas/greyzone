import immutable from 'immutable';

const handyKit = immutable.fromJS({
  description: 'adds 1 electronics dice to throw',
  heavy: false,
  name: 'Handy Kit',
  price: 500,
  tag: 'E1E'
});

export default handyKit;
