import * as state from 'client/state';
import * as agentActions from 'client/agents/actions';
import {expect} from 'chai';

describe('assignMission ', () => {
  it('is a function', () => {
    // TODO: Add real test.
    expect(agentActions.assignMission).to.be.a('function');
  });
});
